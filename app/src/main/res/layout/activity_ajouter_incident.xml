<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/back4"
    android:padding="16dp">

    <!-- En-tête avec icône et titre -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="24dp">

        <ImageView
            android:layout_width="41dp"
            android:layout_height="45dp"
            android:layout_marginEnd="16dp"
            android:contentDescription="Icône problème"
            android:src="@drawable/icproblem"
            android:tint="#4CAF50"
            tools:ignore="UseAppTint" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Décrire votre problème"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:shadowColor="#000000"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="4" />
    </LinearLayout>

    <!-- Champ pour description -->
    <EditText
        android:id="@+id/etDescriptionIncident"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Exemple : Mot de passe oublié"
        android:inputType="textMultiLine"
        android:minHeight="120dp"
        android:gravity="top"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:padding="12dp"
        android:layout_marginBottom="16dp" />

    <!-- Bouton Ajouter Incident avec design moderne -->
    <Button
        android:id="@+id/btnAjouterIncident"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ajouter Incident"
        android:background="@drawable/button_background"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:layout_marginTop="16dp"
        android:elevation="6dp"
        android:drawableStart="@drawable/ic_add"
        android:drawablePadding="8dp" />
</LinearLayout>